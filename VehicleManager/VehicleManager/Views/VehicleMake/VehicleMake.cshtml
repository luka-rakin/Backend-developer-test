@{
    ViewData["Title"] = "Vehicle make";
    var selectedSortBy = Context.Request.Query["sortBy"].ToString();
    var sortOptions = new Dictionary<string, string>
    {
        { "NameAsc", "Sort by Name (A-Z)" },
        { "NameDesc", "Sort by Name (Z-A)" },
        { "AbrvAsc", "Sort by Abrv (A-Z)" },
        { "AbrvDesc", "Sort by Abrv (Z-A)" }
    };

    var successMessage = TempData["SuccessMessage"] as string;
    var errorMessage = TempData["ErrorMessage"] as string;
}

@model VehicleManager.DTO.PagedResult<VehicleManager.DTO.VehicleMakeDto>

<div class="text-center">
    <h1 class="display-4">Vehicle make</h1>
    <hr/>
    <a asp-controller="VehicleMake" asp-action="VehicleMakeForm" class="btn btn-primary mt-3">+ Add new vehicle make</a>


    
    <div class="d-flex justify-content-between">

        <!-- Form for sorting -->
        <form asp-action="VehicleMake" method="get" class="d-flex align-items-center">
            <select id="sortBy" name="sortBy" class="form-select w-auto d-inline" onchange="this.form.submit()">
                @foreach (var option in sortOptions)
                {
                    <option value="@option.Key" selected="@(option.Key == selectedSortBy ? "selected" : null)">
                        @option.Value
                    </option>
                }
            </select>
            <input type="hidden" name="pageSize" value="@Model.PageSize" />
            <input type="hidden" name="pageNumber" value="1" />
        </form>

        <!-- Page Size Dropdown -->
        <form asp-action="VehicleMake" class="mt-3 mb-3">
            <label for="pageSize" class="form-label">Items per page:</label>
            <select id="pageSize" name="pageSize" class="form-select w-auto d-inline" onchange="this.form.submit()">
                @foreach (var size in new int[] { 5, 10, 15, 20, 25, 30 })
                {
                    if (size == Model.PageSize)
                    {
                        <option value="@size" selected>@size</option>
                    }
                    else
                    {
                        <option value="@size">@size</option>
                    }
                }
            </select>
            <input type="hidden" name="pageNumber" value="1" />
        </form>

        
    </div>

    <div>
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Abrv</th>
                    
                </tr>
            </thead>
            <tbody>
                @{
                    foreach(var vehicleMake in Model.Items){
                        <tr>
                            <td>@vehicleMake.Name</td>
                            <td>@vehicleMake.Abrv</td>
                            <td>
                                <form asp-controller="VehicleMake" asp-action="DeleteVehicleMake">
                                    <input type="hidden" name="id" value="@vehicleMake.Id"/>
                                    <button type="submit" class="btn btn-danger">Delete</button>
                                </form>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>


        <!-- Pagination -->
        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center">

                <!-- Previous Button -->
                <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : "")">
                    <a class="page-link" asp-action="VehicleMake" asp-route-pageNumber="@(Model.CurrentPage - 1)" asp-route-pageSize="@Model.PageSize" asp-route-sortBy="@selectedSortBy">Previous</a>
                </li>

                <!-- Page Numbers -->
                @for (int i = 1; i <= Model.TotalPages; i++)
                {
                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                        <a class="page-link" asp-action="VehicleMake" asp-route-pageNumber="@i" asp-route-pageSize="@Model.PageSize" asp-route-sortBy="@selectedSortBy">@i</a>
                    </li>
                }

                <!-- Next Button -->
                <li class="page-item @(Model.CurrentPage == Model.TotalPages ? "disabled" : "")">
                    <a class="page-link" asp-action="VehicleMake" asp-route-pageNumber="@(Model.CurrentPage + 1)" asp-route-pageSize="@Model.PageSize" asp-route-sortBy = "@selectedSortBy">Next</a>
                </li>
            </ul>
        </nav>
    </div>

    @if (!string.IsNullOrEmpty(successMessage))
    {
        <div class="alert alert-success">
            @successMessage
        </div>
    }

    @if (!string.IsNullOrEmpty(errorMessage))
    {
        <div class="alert alert-danger">
            @errorMessage
        </div>
    }

</div>